import React from 'react'
import ReactDOM from 'react-dom/client'
import RecoveryApp from '../irec-recovery-map.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
      <RecoveryApp />
        </React.StrictMode>
        )
